#!/bin/bash
ls -l -a  # Move to the parent directory cd ..  # List files in the parent directory echo "Parent directory:" ls -l -a  # Move back to the original directory cd -  # List files in the /boot directory echo "/boot directory:" ls -l -a /boot Save the above script into a file, for example, list_files.sh. Make the file executable by running the following command in the terminal:  bash Copy code chmod +x list_files.sh Then, you can run the script using the following command:  bash Copy code ./list_files.sh It will display the files in the current directory, followed by the parent directory, and then the /boot directory, all in long format.      